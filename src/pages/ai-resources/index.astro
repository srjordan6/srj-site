---
// The AI Resources hub: srjconsultingservices.com/ai-resources/
// Everything below this layer per the July 27 IA decision, with one
// exception fixed by the same decision: /ai-governance/ stays at root
// and is featured here as the flagship card.
import BaseLayout from '../../layouts/BaseLayout.astro';

const sections = [
  { href: '/ai-governance/', title: 'AI Governance Reference Library', desc: 'Laws, regulations, standards, and enforcement, maintained against primary sources. The flagship: 60+ pages, 82,000+ words.', live: true, flagship: true },
  { href: '/ai-resources/ai-glossary/', title: 'AI Glossary', desc: '522 terms across governance, security, and operations, defined in plain business English.', live: false, note: 'Moves here from /resources/ai-glossary/ at stage 2.' },
  { href: '/ai-resources/ai-news/', title: 'AI News', desc: 'What changed and when: a verified feed of governance developments, every item carrying its primary source.', live: false, note: 'Comes online with the autonomous pipeline.' },
  { href: '/ai-resources/ai-people/', title: 'AI People', desc: 'The regulators, officials, and authors who shape AI governance, with verified roles and records.', live: false, note: 'Graph ring 1.' },
  { href: '/ai-resources/ai-events/', title: 'AI Events', desc: 'Deadlines, effective dates, comment windows, and hearings, the dates that carry legal consequence.', live: false, note: 'Graph ring 1.' },
  { href: '/ai-resources/everything-else/', title: 'AI Tools & Everything Else', desc: 'The 320-tool catalog and every resource that fits nowhere else, verified before it is listed.', live: false, note: 'Catalog migrates at stage 2.' },
];
---
<BaseLayout
  title="AI Resources | SRJ Consulting & Services"
  description="The SRJ AI resource collection: the AI Governance Reference Library, AI glossary, verified AI news, people, events, and tools."
>
  <section class="hub-hero">
    <div class="container">
      <p class="eyebrow">The Collection</p>
      <h1>AI Resources</h1>
      <p class="lede">
        One place for everything the practice publishes: governance law, definitions, verified
        news, the people and dates that matter, and the tools market. Collected as widely as
        possible, published only when verified.
      </p>
    </div>
  </section>
  <section class="container">
    <div class="grid">
      {sections.map((s) => (
        <a class={`card ${s.flagship ? 'flag' : ''}`} href={s.href}>
          <h3>{s.title}</h3>
          <p>{s.desc}</p>
          {s.live ? <span class="badge live">Live</span> : <span class="badge">Coming online</span>}
          {s.note && <span class="note">{s.note}</span>}
        </a>
      ))}
    </div>
  </section>
</BaseLayout>
<style>
  .hub-hero { background: linear-gradient(180deg, var(--cream), var(--white)); padding: 84px 0 48px; border-bottom: 1px solid var(--line); margin-bottom: 48px; }
  .lede { max-width: 68ch; font-size: 19px; color: var(--ink-soft); margin: 0; }
  .grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(320px, 1fr)); gap: 22px; }
  .card { display: block; padding: 26px; border: 1px solid var(--line); border-radius: var(--radius); text-decoration: none; background: var(--white); transition: box-shadow .15s ease, transform .15s ease; }
  .card:hover { box-shadow: var(--shadow-card); transform: translateY(-2px); }
  .card.flag { border: 2px solid var(--orange); grid-column: 1 / -1; background: linear-gradient(135deg, var(--cream), var(--white)); }
  .card h3 { font-size: 20px; margin-bottom: 8px; }
  .card p { font-size: 14.5px; color: var(--ink-soft); margin: 0 0 14px; }
  .badge { display: inline-block; font-size: 11.5px; font-weight: 600; letter-spacing: .08em; text-transform: uppercase; color: var(--gray); border: 1px solid var(--line); padding: 3px 10px; border-radius: 20px; margin-right: 10px; }
  .badge.live { color: var(--white); background: var(--orange); border-color: var(--orange); }
  .note { font-size: 12.5px; color: var(--gray); }
</style>
