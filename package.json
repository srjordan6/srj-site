{
  "name": "srj-site",
  "type": "module",
  "version": "0.1.0",
  "description": "srjconsultingservices.com - static site (Astro on Cloudflare Pages)",
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/fetch-content.mjs && node scripts/make-chrome.mjs && astro build && pagefind --site dist --force-language en",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^4.16.0",
    "three": "^0.168.0"
  },
  "devDependencies": {
    "pagefind": "^1.1.0"
  }
}
